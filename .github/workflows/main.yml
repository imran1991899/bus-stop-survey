name: Stealth Keep-Alive

on:
  schedule:
    # Pick ONE of these and delete the others, or GitHub will run all three.
    # - cron: '25 * * * *'  # Runs at :25 every hour
    # - cron: '35 * * * *'  # Runs at :35 every hour
    - cron: '45 * * * *'    # Runs at :45 every hour
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Instant Stealth Ping
        run: |
          curl -s -L \
            -H "User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/122.0.0.0 Safari/537.36" \
            -H "Referer: https://www.google.com/" \
            "https://bus-stop-survey-dpl6qeby3stuvpiexjhovk.streamlit.app/" > /dev/null
          
          echo "Ping sent instantly at $(date)"
